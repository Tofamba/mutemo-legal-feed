# ── Scraping ───────────────────────────────────────────────────────────────────
# Scraping runs via Crawl4AI (self-hosted, Playwright-based) — no API key needed.
# (Previously used Firecrawl.dev; migrated off it, no FIRECRAWL_API_KEY required.)

# ── Firm registry ──────────────────────────────────────────────────────────────
# Each firm this feed pushes to needs its own FIRM_{N}_* block.
# Supports up to 10 firms via FIRM_1_* through FIRM_10_*.
FIRM_1_BASE_URL=https://mutemo-sawyer.up.railway.app
FIRM_1_ADMIN_TOKEN=your-firm-1-admin-token-here
FIRM_1_ID=a1b2c3d4-0000-0000-0000-000000000001   # Sawyer & Mkushi firm UUID
FIRM_1_NAME=Sawyer & Mkushi

FIRM_2_BASE_URL=https://mutemo-legalcorner.up.railway.app
FIRM_2_ADMIN_TOKEN=your-firm-2-admin-token-here
FIRM_2_ID=b2c3d4e5-0000-0000-0000-000000000002   # Legal Corner firm UUID
FIRM_2_NAME=Legal Corner

# Legacy fallback (only used if no FIRM_1_* vars are set):
# MUTEMOS_BASE_URL=https://mutemoos-production.up.railway.app
# MUTEMOS_ADMIN_TOKEN=your-mutemos-admin-token-here
# MUTEMOS_FIRM_ID=a1b2c3d4-0000-0000-0000-000000000001

# ── Laws.Africa Knowledge Base API ─────────────────────────────────────────────
# Free tier: sign up at https://laws.africa — no scraping credit cost, REST API.
# Covers judgments-zw and legislation-zw. Runs weekly (Sundays).
LAWS_AFRICA_TOKEN=your-laws-africa-token-here

# ── Feed service auth ──────────────────────────────────────────────────────────
# Protects the /trigger/* and /failures endpoints
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
FEED_ADMIN_TOKEN=your-feed-admin-token-here

# ── Storage ────────────────────────────────────────────────────────────────────
# Path to persistent volume on Railway (mount a volume at /data — without this,
# state resets on every redeploy and every URL looks "unseen" again).
DATA_DIR=/data

# ── Dry run ────────────────────────────────────────────────────────────────────
# Set to "true" to scrape and log without pushing to MutemoOS.
# Use this for initial testing before going live.
DRY_RUN=false